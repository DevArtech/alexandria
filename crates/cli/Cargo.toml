[package]
name = "alexandria"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Alexandria CLI — local-first LLM memory"

[[bin]]
name = "alexandria"
path = "src/main.rs"

[dependencies]
alexandria-core = { path = "../core", version = "0.1.0" }
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde_json = "1"
